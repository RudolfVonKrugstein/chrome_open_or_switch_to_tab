{
  "manifest_version": 3,
  "name": "TabController",
  "description": "Actvate a Tab based on a regex or open a new tab if no tab matches the regex.\n\nThis extension uses the options page to either switch to a tab with your desired URL already open or redirect you to the url of no tab matches.",
  "version": "1",
  "author": "Nathan Hüsken",
  "background": {
    "service_worker": "background.js"
  },
  "permissions": [
    "tabs"
  ],
  "options_page": "options.html"
}
